double calcRPerimeter(double length, double width) {
	double perimeter = 2 * (length + width);
	return perimeter;
}

double calcRArea(double length, double width) {
	double area = length * width;
	return area;
}
